# Set `top` variable to true

Contract did not implement `isLastFloor` so we can implement and invoke `goTo`

Our Solution will return `false` first then call again with a random number will return `true`

Verify:
```
contract.top()
```